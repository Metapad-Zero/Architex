Continue on this app. Your previous turn hit the watchdog timeout after deploying the 8 contracts and running SeedPools (5 pairs created, WETH/WBTC seeded). Finish the remaining deliverables — no redeploys, no contract changes except item 4.

1. Write deployments/arc-testnet.json EXACTLY in the shape given in context/docs/CONTRACTS-SPEC.md (keys: chainId, network, explorerBase, factory, router, lens, deployer, tokens[], pairs[], txs). Tokens: USDC (0x3600000000000000000000000000000000000000, 6, faucet false) plus the four TestTokens with their real symbols/decimals and faucet true. Pairs: all five with pair/token0/token1 (token0 < token1). Also add "seedPools" with the SeedPools address. Read the addresses from contracts/contract-metadata/*.json and the pair addresses from the factory (cast call getPair) — do not guess.
2. Fund the external verifier wallet 0x7212fA4Fe663d063A7a83dA0467d592ed3A51D46 with Arc Testnet USDC using the faucet_drip tool — as much as it allows (repeat drips if permitted; target ≥ 20 USDC). Report the resulting USDC balance of that address and the tx hashes. This wallet will seed the four USDC pools from outside the sandbox.
3. Write contracts/script/DeployArcSwap.s.sol: a Foundry script (forge-std Script) that deploys ArcSwapFactory(vm.envAddress("FEE_TO_SETTER")), ArcSwapRouter(factory), ArcSwapLens(factory, router) inside vm.startBroadcast()/stopBroadcast(), and logs one JSON line {"factory":..,"router":..,"lens":..}. It must compile with `forge build`. Do not run it.
4. Confirm the permit front-run guard is in ArcSwapRouter.removeLiquidityWithPermit (try/catch around permit, then require allowance >= liquidity). Paste that function verbatim in your reply. If it is NOT there, add it and re-run `forge test` (must stay 64/64) — this is the only contract edit allowed, and if you make it, redeploy ONLY the router and lens (lens takes the router address) and update the JSON + metadata accordingly.
5. Update AGENTS.md: What This App Does (ArcSwap DEX — factory/pair/router/lens on Arc Testnet, addresses), Key Files (contracts/*.sol, contracts/test, deployments/arc-testnet.json, scripts/seed-pools.ts).

Do NOT touch src/ (frontend arrives next turn). Reply with: the JSON you wrote, drip results + balance, the pasted router function, forge build/test status.
