Build the Architex launchpad contracts in this app (the Architex AMM you wrote is already here).

Read docs/launchpad/LAUNCHPAD-SPEC.md first: it is binding. The ABI in contracts/interfaces/IArchitexLaunchpad.sol and contracts/interfaces/ILaunchToken.sol is FROZEN (the frontend is being built against it): do not change those two files. If you think the spec is wrong anywhere, implement it as written and list your objections in your final message.

Deliverables, default preset (not max), no deployment in this turn:
1. contracts/launchpad/ArchitexLaunchpad.sol implementing IArchitexLaunchpad, and contracts/launchpad/LaunchToken.sol implementing ILaunchToken. Solidity 0.8.28, OpenZeppelin 5.1.0, evm paris, via_ir, same foundry.toml. ReentrancyGuard + SafeERC20, checks-effects-interactions, custom errors from the interfaces, NatSpec on every external function.
2. The curve math exactly as specified (constant product on virtual reserves, k recomputed each trade, ceil divisions where the spec says ceil, rounding always in favour of the curve, exact-fill on the buy that sells out the curve: pull only net+fee, never pull-then-refund).
3. Graduation exactly as specified: inside the sell-out buy, atomically; token.markGraduated(); transfer POOL_SUPPLY tokens and all the curve's real USDC straight to the pair; pair.mint(DEAD). Do NOT use the router. createToken must use factory.getPair and only call createPair when the pair does not exist yet.
4. contracts/test/ArchitexLaunchpad.t.sol using the REAL ArchitexFactory/ArchitexPair from this repo and a 6-decimal mock USDC. One test per bullet of the spec's "Security requirements", including: fuzzed buy/sell round trips never profit; invariant test for solvency (USDC held >= sum of virtualUsdc - VIRTUAL_USDC_0 over live curves), k non-decreasing, tokensSold <= CURVE_SUPPLY; graduation when the pair was pre-created by an attacker; graduation when an attacker donated USDC to the pair and called sync(); LP tokens end at DEAD; pool opening price equals the curve's final price within 1e-6 relative; transfers to the pair revert before graduation and work after; launchpadPull/initPair/markGraduated access control; exact-fill leaves zero unsold dust and never over-charges; fee goes to feeTo on buys, sells and the launch fee; name/symbol/metadata length limits; admin setters and their limits.
5. Run forge build and forge test. All existing Architex tests must still pass. Report the final test counts.

Context: another reviewer (Grok Build) is attacking this spec in parallel. A follow-up turn will bring their findings for you to fix, and only then deploy to Arc testnet (usdc 0x3600000000000000000000000000000000000000, factory 0x6362f5A0fc007AB7D1e61f99D3F4eB04360D060a).
